{% extends "base/basePage.html.twig" %}

{% block title %}404 | Lucas Bruel
{% endblock %}

{% block body %}
	<section class="mx-auto flex min-h-[70vh] max-w-6xl flex-col items-center justify-center px-6 py-16 text-center">
		<p class="text-sm font-bold uppercase tracking-[0.2em] text-cyan-300">404</p>
		<h1 class="mt-4 text-4xl font-extrabold text-white md:text-6xl">
			{{ 'error.project_not_found'|trans }}
		</h1>
		<p class="mt-4 max-w-lg text-slate-300">
			{{ 'error.project_unavailable'|trans }}
		</p>
		<a href="{{ path('homepage', { _locale: app.request.locale|default('fr') }) }}#projects" class="mt-8 inline-flex items-center rounded-md bg-cyan-500 px-4 py-2 text-sm font-semibold text-slate-950 transition hover:bg-cyan-300 focus:outline-none focus:ring-2 focus:ring-cyan-400">
			{{ 'error.back_to_projects'|trans }}
		</a>
	</section>
{% endblock %}
