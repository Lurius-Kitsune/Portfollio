{# Project card component #}
{% trans_default_domain 'project' %}


<article class="group overflow-hidden rounded-lg bg-slate-800 shadow-lg ring-1 ring-slate-700/80 transition-all duration-500 ease-out hover:-translate-y-1 hover:shadow-2xl hover:shadow-cyan-500/10">
	<div class="block">
		<div class="h-40 overflow-hidden bg-slate-700">
			{% if project.thumbnailUrl is not empty %}
                <img src="{% if project.thumbnailUrl starts with 'http://' or project.thumbnailUrl starts with 'https://' %}{{ project.thumbnailUrl }}{% else %}{{ asset(project.thumbnailUrl) }}{% endif %}" class="h-full w-full object-cover transition-transform duration-500 ease-out group-hover:scale-105">
			{% else %}
                <img src="{{ asset('images/nothumbnail.gif') }}" alt="Aucune Image" class="h-full w-full object-cover transition-transform duration-500 ease-out group-hover:scale-105">
			{% endif %}
		</div>
		<div class="p-4 transition-all duration-300 ease-out">
			<h3 class="text-lg font-semibold text-white">{{ project.name }}</h3>
			<!--<p class="mt-2 text-sm text-slate-300">project.description</p>-->

			<div class="mt-4 flex flex-wrap gap-2">
				{% for tag in project.tags|default([]) %}
					<span class="rounded-full bg-slate-700 px-3 py-1 text-[11px] text-slate-300">{{ tag }}</span>
				{% endfor %}
			</div>

				<div class="mt-4 flex items-center justify-between gap-2">
                <span class="text-xs text-slate-400">{{ project.role }}
	•
	{{ project.startYear ? project.startYear|date('m/Y') : '' }}</span>

                {% if project.projectContents is not empty %}

<a href="{{ path('project_show', { _locale: app.request.locale|default('fr'), slug: project.slug }) }}" class="inline-flex items-center rounded-md bg-cyan-500 px-3 py-2 text-sm font-semibold text-slate-950 transition hover:bg-cyan-300 focus:outline-none focus:ring-2 focus:ring-cyan-400">

{{ locale == 'en' ? 'Details' : 'Détails' }}</a>

{% else %}
<span class="inline-flex items-center rounded-md bg-slate-700 px-3 py-2 text-sm font-semibold text-slate-400 cursor-not-allowed opacity-60" aria-disabled="true">
	{{ locale == 'en' ? 'Details' : 'Détails' }}
</span>{% endif %}

</div>

		</div>
	</div>
</article>
